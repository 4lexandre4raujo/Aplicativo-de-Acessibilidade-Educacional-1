import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aplicativo-de-acessibilidade-educacional-1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
